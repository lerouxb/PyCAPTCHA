Known bugs:

- PersistentFactory() is almost certainly horrible at concurrent access
- Tests are never invalidated with PersistentStorage(), as they aren't written back to the database
- Data files aren't installed correctly


